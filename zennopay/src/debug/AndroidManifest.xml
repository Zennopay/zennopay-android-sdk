<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG-variant additions only: the screen-gallery activity used by the
  accessibility / overflow / screen-size QA passes. Never merged into release.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.zennopay.sdk.debug.ZennopayDebugGalleryActivity"
            android:exported="true"
            android:theme="@style/Theme.Zennopay.Checkout"
            android:configChanges="orientation|screenSize|keyboardHidden|uiMode" />
    </application>
</manifest>
